# @package _global_

# to execute this experiment run:
# python run.py experiment=example_simple.yaml

defaults:
  - /experiment/imagenet/mixer/mixerb.yaml

train:
  mixup:
    cutmix_alpha: 0.6
  optimizer:
    weight_decay: 0.05
